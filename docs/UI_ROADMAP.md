# UI roadmap

Frontend polish and interaction ideas not yet built. Separate from `ROADMAP.md`
(backend/architecture phases) so cosmetic requests don't get lost in engineering
milestones.

## Backlog

- **Grid hover effect on buttons.** A grid-line/grid-pattern treatment that
  appears across a button on hover. Request came in cut off mid-sentence --
  confirm the exact look before building: a subtle grid overlay across the
  button face, vs. a grid-aligned border/glow, vs. something else. Which
  buttons (all `.btn` variants, or a specific set like the composer/send
  button) is also open.
